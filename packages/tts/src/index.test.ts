/**
 * @spec docs/specs/P0.A-4-vitest-baseline.md
 * Smoke test — Phase 1 Web Speech / Phase 2 OpenAI TTS 封装接入时会替换本文件。
 */
import { describe, expect, it } from "vitest";

describe("@aiclassroom/tts — smoke", () => {
  it("1 + 1 = 2", () => {
    expect(1 + 1).toBe(2);
  });
});
